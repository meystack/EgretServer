#pragma once
/*
*这个文件为导出到脚本的一些数据结构，一些接口太复杂，而且不方便导出，可以放到这里单独导出
*可能包含类似vector的数据结构
*/

/*
template<class T>
class CVector
{
public:
	TEMPLATE_BIND(void * pPack,T, int, double);

	void empty(void * pPack,);
	void clear(void * pPack,);
	int count(void * pPack,) const;

	const T& get(void * pPack,int index) const;
	void set(void * pPack, int index, const T item);
	void remove(void * pPack,int index);
	void add(void * pPack,T val);

	CVector(void * pPack,);
	~CVector(void * pPack,);
};
*/




